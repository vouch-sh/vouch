{
  "description": "vouch-server/{VERSION} (https://github.com/vouch-sh/vouch)",
  "variant": {
    "openid": "openid_connect",
    "client_auth_type": "mtls",
    "sender_constrain": "dpop",
    "fapi_profile": "plain_fapi"
  },
  "client_alias": "vouch-fapi2-sp-mtls-dpop",
  "server": {
    "discoveryUrl": "{BASEURL}/.well-known/openid-configuration"
  },
  "client": {
    "client_id": "{CLIENT_ID}",
    "jwks": {CLIENT_JWKS},
    "scope": "openid email",
    "registration_access_token": "{CLIENT_REG_TOKEN}",
    "tls_client_auth_subject_dn": "{TLS_CLIENT_AUTH_SUBJECT_DN}"
  },
  "mtls": {
    "cert": "{MTLS_CERT}",
    "key": "{MTLS_KEY}"
  },
  "resource": {
    "resourceUrl": "{BASEURL}/oauth/userinfo"
  },
  "client2": {
    "client_id": "{CLIENT2_ID}",
    "jwks": {CLIENT2_JWKS},
    "scope": "openid email",
    "registration_access_token": "{CLIENT2_REG_TOKEN}",
    "tls_client_auth_subject_dn": "{TLS_CLIENT_AUTH_SUBJECT_DN2}"
  },
  "mtls2": {
    "cert": "{MTLS2_CERT}",
    "key": "{MTLS2_KEY}"
  },
  "browser": [
    {
      "match": "{BASEURL}/oauth/authorize*",
      "tasks": [
        {
          "task": "Login",
          "optional": true,
          "match": "{BASEURL}/login*",
          "commands": [
            ["click", "id", "cert-login"]
          ]
        },
        {
          "task": "Verify Complete",
          "optional": true,
          "match": "*/test/*/callback*",
          "commands": [
            ["wait", "id", "submission_complete", 10]
          ]
        }
      ]
    }
  ],
  "override": {
    "fapi2-security-profile-final-user-rejects-authentication": {
      "browser": [
        {
          "match": "{BASEURL}/oauth/authorize*",
          "tasks": [
            {
              "task": "Deny Login",
              "optional": true,
              "match": "{BASEURL}/login*",
              "commands": [
                ["click", "id", "cert-deny"]
              ]
            },
            {
              "task": "Verify Complete",
              "optional": true,
              "match": "*/test/*/callback*",
              "commands": [
                ["wait", "id", "submission_complete", 10]
              ]
            }
          ]
        }
      ]
    },
    "fapi2-security-profile-final-par-ensure-reused-request-uri-prior-to-auth-completion-succeeds": {
      "browser": [
        {
          "match": "{BASEURL}/oauth/authorize*",
          "match-limit": 1,
          "tasks": []
        },
        {
          "match": "{BASEURL}/oauth/authorize*",
          "tasks": [
            {
              "task": "Login",
              "optional": true,
              "match": "{BASEURL}/login*",
              "commands": [
                ["click", "id", "cert-login"]
              ]
            },
            {
              "task": "Verify Complete",
              "optional": true,
              "match": "*/test/*/callback*",
              "commands": [
                ["wait", "id", "submission_complete", 10]
              ]
            }
          ]
        }
      ]
    }
  }
}
