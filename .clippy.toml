# Clippy configuration for the vouch workspace.
# See: https://doc.rust-lang.org/clippy/configuration.html

# Request-path time comparisons in vouch-server read one instant per request
# (`vouch_server::arrival::ArrivalTime`) rather than stamping their own clock,
# so two comparisons serving one decision cannot observe different instants.
#
# The lint LEVEL is `allow` workspace-wide (see `[workspace.lints.clippy]` in
# Cargo.toml) and raised to `warn` only in vouch-server's crate roots: the CLI
# and agent have no request to anchor to. Within vouch-server, ambient stamping
# is still right in four places, each carrying an `#[expect]` that names which:
# the arrival middleware, OCC retry closures, db row stamping, and background
# tasks.
disallowed-methods = [
    { path = "jiff::Timestamp::now", reason = "request-path comparisons take `crate::arrival::ArrivalTime`; see `arrival.rs` for the sanctioned ambient-stamping cases" },
]

# Items from our own crates are brought in with `use`, not spelled inline as
# `crate::db::users::get(..)` or `vouch_common::paths::config_dir()`: import the
# type, trait, or const directly, and import a function's parent module
# (`use crate::db;` then `db::create_session(..)`). `clippy::absolute_paths` flags
# any path of more than two segments rooted at a crate; every external crate is
# listed below so the lint reports only `crate::` and `vouch_*::` paths.
#
# The lint LEVEL is `allow` workspace-wide and raised to `deny` at the root of
# each crate that has been migrated. A new external dependency named by an
# absolute path in a migrated crate belongs in this list.
absolute-paths-max-segments = 2
absolute-paths-allowed-crates = [
    "std",
    "core",
    "alloc",
    "proc_macro",
    "anyhow",
    "apple_native_keyring_store",
    "arc_swap",
    "askama",
    "aws_config",
    "aws_lc_rs",
    "aws_sdk_dsql",
    "aws_sdk_kms",
    "aws_sdk_s3",
    "aws_sdk_ssm",
    "aws_smithy_types",
    "axum",
    "axum_extra",
    "axum_server",
    "base64",
    "ciborium",
    "clap",
    "clap_complete",
    "const_oid",
    "ctap_hid_fido2",
    "der",
    "dirs",
    "dogwood_language",
    "dotenvy",
    "ed25519_dalek",
    "flate2",
    "fluent_bundle",
    "gethostname",
    "governor",
    "hex",
    "hickory_resolver",
    "http",
    "i18n_embed",
    "i18n_embed_fl",
    "idna",
    "inquire",
    "ipnet",
    "jiff",
    "jsonwebtoken",
    "keyring_core",
    "libc",
    "linux_keyutils_keyring_store",
    "maxminddb",
    "metrics",
    "metrics_exporter_prometheus",
    "mimalloc",
    "open",
    "opentelemetry",
    "opentelemetry_otlp",
    "opentelemetry_sdk",
    "p256",
    "proptest",
    "psl",
    "reqwest",
    "roxmltree",
    "rpassword",
    "rust_embed",
    "rust_ini",
    "rustls",
    "sea_query",
    "sea_query_sqlx",
    "secrecy",
    "serde",
    "serde_json",
    "serde_saphyr",
    "serde_urlencoded",
    "sha2",
    "signature",
    "spki",
    "sqlx",
    "ssh_key",
    "subtle",
    "sys_locale",
    "tempfile",
    "thiserror",
    "time",
    "tokio",
    "tokio_rustls",
    "tokio_util",
    "toml_edit",
    "tower",
    "tower_governor",
    "tower_http",
    "tracing",
    "tracing_opentelemetry",
    "tracing_subscriber",
    "unic_langid",
    "url",
    "urlencoding",
    "uuid",
    "webauthn_rs",
    "webauthn_rs_proto",
    "which",
    "windows",
    "windows_native_keyring_store",
    "winresource",
    "wiremock",
    "x509_cert",
    "zeroize",
]

# Allow breaking the exported API (we're pre-1.0)
avoid-breaking-exported-api = false

# Cognitive complexity threshold
cognitive-complexity-threshold = 30

# Maximum lines in a function before triggering too_many_lines
too-many-lines-threshold = 150

# Allow certain common names for single-char variables
allowed-idents-below-min-chars = ["x", "y", "z", "i", "j", "k", "n", "s", "e"]

# MSRV comes from each package's `rust-version`, inherited via
# `rust-version.workspace = true`. Clippy skips MSRV-gated lints without it.
